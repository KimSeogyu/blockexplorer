/** @type {import('./$types').Actions} */
export const actions = {
  blocks: async (event) => {
    console.log(event);
    return event;
  },

  transactions: async (event) => {
    console.log(event);
    return event;
  },

  sbt: async (event) => {
    console.log(event);
    return event;
  },

  did: async (event) => {
    console.log(event);
    return event;
  },
};
